const password = 'pas$word';

password.length >= 12 ? console.log('password is very strong so Welcome!!!') :
password.lenght >=8 ? console.log('Welcome!!!') :
console.log('Sorry!!! the password is too short');
